
name: chapter_16
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  dio: ^5.5.0+1
  intl: ^0.20.2
  flutter_riverpod: ^2.6.1

  firebase_core: ^4.7.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - CROSSFINAL/assets/shoes/
